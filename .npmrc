# CRITICAL FIX: Enable legacy peer dependency resolution for React 19 compatibility
legacy-peer-deps=true

# Optimize npm for production builds
fund=false
audit=false

# Improve build performance
prefer-offline=true
progress=false

# Security and reliability
save-exact=true 