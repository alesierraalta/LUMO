#!/usr/bin/env node

/**
 * Environment Switcher for LUMO Inventory Management System
 * Switches between development and production Supabase configurations
 */

const fs = require('fs');
const path = require('path');

const environments = {
  dev: {
    NEXT_PUBLIC_SUPABASE_URL: 'https://ndprriqyhddjoixrlqnz.supabase.co',
    NEXT_PUBLIC_SUPABASE_ANON_KEY: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im5kcHJyaXF5aGRkam9peHJscW56Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTAxMDg0MDAsImV4cCI6MjA2NTY4NDQwMH0.4rzi6UFGnN6ien_706ETHjBylZMK6jt0vjRvvnJ1J-8',
    NODE_ENV: 'development'
  },
  prod: {
    NEXT_PUBLIC_SUPABASE_URL: 'https://ubjujxtvlubxowsphvuk.supabase.co',
    NEXT_PUBLIC_SUPABASE_ANON_KEY: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InVianVqeHR2bHVieG93c3BodnVrIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NDk1MTIzODQsImV4cCI6MjA2NTA4ODM4NH0.SapRqhZCDJypL1fMCiEChK0ehZRR5CSI1fRgt3Za8r4',
    NODE_ENV: 'production'
  }
};

const envFile = path.join(process.cwd(), '.env.local');

function switchEnvironment(env) {
  if (!environments[env]) {
    console.error(`❌ Invalid environment: ${env}`);
    console.log('Available environments: dev, prod');
    process.exit(1);
  }

  const config = environments[env];
  const envContent = `# LUMO Inventory Management System - ${env.toUpperCase()} Environment
# Generated by switch-environment.js on ${new Date().toISOString()}

NEXT_PUBLIC_SUPABASE_URL=${config.NEXT_PUBLIC_SUPABASE_URL}
NEXT_PUBLIC_SUPABASE_ANON_KEY=${config.NEXT_PUBLIC_SUPABASE_ANON_KEY}
NODE_ENV=${config.NODE_ENV}
FORCE_SUPABASE=true

# Database Info:
# DEV: LUMO dev (ndprriqyhddjoixrlqnz) - Fresh schema with test data
# PROD: LUMO (ubjujxtvlubxowsphvuk) - Your production data with alesierraalta@gmail.com
`;

  try {
    fs.writeFileSync(envFile, envContent);
    console.log(`✅ Successfully switched to ${env.toUpperCase()} environment`);
    console.log(`📁 Updated: ${envFile}`);
    
    if (env === 'dev') {
      console.log(`🔗 Database: LUMO dev (ndprriqyhddjoixrlqnz.supabase.co)`);
      console.log(`👤 Login: admin@lumo.com / admin123`);
    } else {
      console.log(`🔗 Database: LUMO production (ubjujxtvlubxowsphvuk.supabase.co)`);
      console.log(`👤 Your account: alesierraalta@gmail.com (ADMIN)`);
    }
    
    console.log(`\n🚀 Restart your development server: npm run dev`);
  } catch (error) {
    console.error(`❌ Failed to write environment file: ${error.message}`);
    process.exit(1);
  }
}

// Parse command line arguments
const args = process.argv.slice(2);
const env = args[0];

if (!env) {
  console.log('🔄 LUMO Environment Switcher');
  console.log('Usage: node scripts/switch-environment.js <env>');
  console.log('');
  console.log('Available environments:');
  console.log('  dev  - Development database (fresh schema)');
  console.log('  prod - Production database (your live data)');
  console.log('');
  console.log('Examples:');
  console.log('  node scripts/switch-environment.js dev');
  console.log('  node scripts/switch-environment.js prod');
  process.exit(0);
}

switchEnvironment(env); 